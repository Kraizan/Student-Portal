import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    btnColor: {
      main: "#f5f5f5",

      dark: "#24a0ed",
    },
    btnGrey: {
      main: "#ececec",
    },
    btnGreen: {
      main: "#00b518",
      dark: "#009113",
    },
    text: {
      main: "#f5f5f5",
      dark: "#404040",
    },
  },
});

export default theme;
